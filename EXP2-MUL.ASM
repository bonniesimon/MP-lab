ASSUME CS:CODE, DS:DATA

DATA SEGMENT
NUM1 DW 1025H
NUM2 DW 0025H
QUO DW 1 DUP(?)
REM DW 1 DUP(?)
DATA ENDS

CODE SEGMENT
START: MOV AX, DATA
       MOV DS, AX

       MOV AX, NUM1
       MOV BX, NUM2
       MUL BX
       MOV QUO, AX
       MOV REM, DX

       MOV AH, 4CH
       INT 21H
CODE ENDS
        END START
