ASSUME DS:DATA, CS:CODE

DATA SEGMENT
NUM DB 0ABH
RESH DW ?
RESL DW ?
DATA ENDS

CODE SEGMENT
START:  MOV AX, DATA
        MOV DS, AX

	MOV DX, 0000H
        MOV BL, NUM
	MOV AL, BL

	MUL BL
	MOV CX, AX
	MOV BH, 00H
	MUL BX

	MOV RESH, DX
	MOV RESL, AX

        MOV AH, 4CH
        INT 21H

CODE ENDS
        END START
